import React from "react";

import "./dashboard-sidebar.style.scss";

const DashboardSidebar = () => {
  return <aside className="dashboard__sidebar">SIDE BAR</aside>;
};

export default React.memo(DashboardSidebar);
